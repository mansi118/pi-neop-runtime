{
  "_comment": "Verifier always fails t1 -> re-plans exhaust the budget -> ESCALATED (criterion 6).",
  "plan": {
    "plan_version": "v1",
    "tasks": [
      {
        "task_id": "t1",
        "description": "find 500 enterprises matching an impossible ICP",
        "depends_on": [],
        "tool": "browser_agent",
        "acceptance": ">=500 deduped rows",
        "scope": "tool"
      }
    ],
    "max_replans": 2
  },
  "verify": {
    "__default__": "fail"
  }
}
